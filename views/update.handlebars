<div>
    <div>
        {{> contactsBtn}}
    </div>
    <form id="form-update">
        <div>
            <input name="id" value="{{contact.id}}" style="display: none">
            <input type="text" name="fullName" value="{{contact.fullName}}" placeholder="ФИО" required>
            <input type="tel" name="phoneNumber" value="{{contact.phoneNumber}}" placeholder="+375291234567"
                pattern="+375[0-9]{2}[0-9]{3}[0-9]{2}[0-9]{2}" title="code_country(code_region)phone_number" required
                autocomplete="off">
        </div>
        <div>
            <button id="update" type="submit">Изменить</button>
            <button id="delete">Удалить</button>
            <button type="button" onclick="{{refuse}}">Отказаться</button>
        </div>
    </form>
</div>